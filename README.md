# Hyperparameter-Tuning
This Streamlit app performs hyperparameter tuning on a Decision Tree using the Iris dataset. It allows users to interactively select values for max_depth and min_samples_split and run both GridSearchCV and RandomizedSearchCV. Results and tested combinations are displayed instantly.
